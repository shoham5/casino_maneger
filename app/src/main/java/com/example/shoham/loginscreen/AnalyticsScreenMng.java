package com.example.shoham.loginscreen;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.TextView;
import android.widget.Toast;

//import com.google.firebase.analytics.FirebaseAnalytics;
//private FirebaseAnalytics mFirebaseAnalytics;


public class AnalyticsScreenMng extends AppCompatActivity {

  //  private FirebaseAnalytics mFirebaseAnalytics;


    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_analytics_screen_mng);


        //TextView id= (TextView) findViewById(R.id.idText);

        // Obtain the FirebaseAnalytics instance.
       // mFirebaseAnalytics = FirebaseAnalytics.getInstance(this);

      //  mFirebaseAnalytics.logEvent();







        /*
        Bundle bundle = new Bundle();
        String id="";
        String name="";
        bundle.putString(FirebaseAnalytics.Param.ITEM_ID, id);
        bundle.putString(FirebaseAnalytics.Param.ITEM_ID, id);
        bundle.putString(FirebaseAnalytics.Param.ITEM_NAME, name);
        bundle.putString(FirebaseAnalytics.Param.CONTENT_TYPE, "image");
        mFirebaseAnalytics.logEvent(FirebaseAnalytics.Event.SELECT_CONTENT, bundle);

           Toast.makeText(AnalyticsScreenMng.this,id, Toast.LENGTH_LONG).show();
        Toast.makeText(AnalyticsScreenMng.this, name, Toast.LENGTH_LONG).show();
        Toast.makeText(AnalyticsScreenMng.this, "))))))))))", Toast.LENGTH_LONG).show();

    */


    }


/*

*/
}
